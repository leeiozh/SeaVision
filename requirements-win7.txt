# Win7-совместимая линия: последние версии, у которых есть cp38 win_amd64 wheels
# и которые НЕ тянут PSS / path-API из kernel32 (Python 3.8 — последний с офиц.
# поддержкой Windows 7). Штатная сборка — БЕЗ matplotlib (pics=false).
numpy==1.24.4
scipy==1.10.1
netCDF4==1.6.5
cftime==1.6.3
